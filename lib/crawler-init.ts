export const initCrawlerSystem = () => {
  if (typeof window === 'undefined') {
    return;
  }

  // Placeholder until crawler runtime is wired in this frontend entrypoint.
  console.log('Crawler system initialization skipped (stub).');
};
